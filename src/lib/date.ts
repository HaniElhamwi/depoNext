export const formatDate = (dateString: string) => {
  // const date = new Date(dateString);
  // return new Intl.DateTimeFormat(locale === "ar" ? "ar-EG" : "tr-TR", {
  //   year: "numeric",
  //   month: "long",
  //   day: "numeric",
  // }).format(date);
  return dateString;
};
